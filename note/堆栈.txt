1.栈与堆都是用来存储数据的Ram.
  //栈运行之前就分配内存，存取速度快，而堆是运行时动态分配内存，存取比较慢。


在方法的参数传递中，基本数据类型，String类是按值传递，即拷贝了一个副本！引用数据类型是按引用传递，即把栈中的地址传入！

栈中保存引用（地址）通过这个地址去得到保存在堆中的对象。
  //常量池，存在于发回去，而方法去存在于堆中、

int a=3;//先创建 a的引用，在栈中查找是否有3，没有就开辟一个内存空间来保存3.a指向3.
int b=3;//先去判断3是否存在，直接将3赋值给b，不用在开辟空间。